[Unit]
Description=Idle system suspend
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/bash -c 'exec swayidle -w timeout 900 "systemctl suspend"'
Restart=on-failure
RestartSec=2

[Install]
WantedBy=graphical-session.target
